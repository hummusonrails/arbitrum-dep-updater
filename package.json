{
  "name": "arbitrum-dep-updater",
  "version": "1.0.0",
  "description": "GitHub Action to automatically check and update Arbitrum-specific dependencies in workshop and dApp repos",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "node --test src/__tests__/"
  },
  "keywords": [
    "arbitrum",
    "stylus",
    "github-action",
    "dependency-updater",
    "viem",
    "wagmi",
    "foundry"
  ],
  "author": "Ben Greenberg",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0",
    "@actions/glob": "^0.5.0",
    "semver": "^7.6.3",
    "smol-toml": "^1.3.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
